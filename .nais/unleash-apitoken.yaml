apiVersion: unleash.nais.io/v1
kind: ApiToken
metadata:
  name: {{name}}
  namespace: bidrag
  labels:
    team: bidrag
spec:
  unleashInstance:
    apiVersion: unleash.nais.io/v1
    kind: RemoteUnleash
    name: bidrag
  secretName: {{name}}-unleash-api-token
  environment: {{unleash.environment}}
