import type {Route} from "../../../../../.react-router/types/app/routes/admin/+types";

export default function VedtakExplorerPage({params}: Route.ComponentProps) {
    console.log(params);
    return (
        <div>VEDTAK EXPLORER PAGE</div>

    );
}
